* linguist-vendored
*.py linguist-vendored=false

# download lfs files:
*.bam filter=lfs diff=lfs merge=lfs -text
*.gz filter=lfs diff=lfs merge=lfs -text
*.gz !text !filter !merge !diff
*.bam !text !filter !merge !diff
